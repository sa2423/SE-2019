package com.Lieyang.waiter.Network;

public enum RequestType {
    GET_CURRENTORDERS,
    GET_COMPLETEDORDERS,
    COMPLETE_ORDER,
    GET_REQUESTS,
    COMPLETE_REQUESTS,
    ORDER_INFO,
    LOGIN,
    LOGOUT
}
